# Release Flow Guide

## 🧠 ภาพรวม

Release flow = กติกาที่บอกว่า "ของเปลี่ยนแค่ไหน → เลขเวอร์อะไร → user จะได้ยังไง"

---

## 1️⃣ SemVer Strategy (หัวใจ)

### SemVer = MAJOR.MINOR.PATCH

เช่น `1.2.3`

| เลข | หมายถึง |
|-----|---------|
| **MAJOR** | ของเก่าพัง (breaking change) |
| **MINOR** | เพิ่มของใหม่ แต่ของเก่าใช้ได้ |
| **PATCH** | แก้บั๊ก |

### สำหรับ CLI ของคุณ (แนะนำ)

**ตอนนี้คุณยัง <1.0.0**

| สิ่งที่คุณทำ | เวอร์ |
|-------------|-------|
| เพิ่ม component | **minor** |
| เพิ่ม flag / config | **minor** |
| แก้บั๊ก add | **patch** |
| เปลี่ยน behavior เดิม | **major** |

📌 **หลัง 1.0.0 ต้องระวัง breaking มาก**

---

## 2️⃣ Changelog (ทำให้ user เชื่อใจ)

### Changelog คืออะไร

ไฟล์ที่บอกว่า: **"เวอร์นี้มีอะไรเปลี่ยน"**

- ไม่ใช่ให้เครื่องอ่าน
- แต่ให้ **คนอ่าน**

### ตัวอย่างที่ดี

```markdown
## 0.3.0

- add dependency system (form → button)
- add zend-theme.config.json

## 0.2.1

- fix overwrite confirm
```

📌 **User จะรู้ทันที:**
- ควรอัปเดตไหม
- มีอะไรพังไหม

---

## 3️⃣ Git Tag (หลักฐานการ release)

### Tag คืออะไร

**"จุดใน git ที่บอกว่า version นี้คือ commit ไหน"**

เช่น:
- `v0.3.0` → commit `abc123`

📌 **สำคัญเพราะ:**
- rollback ได้
- debug เวอร์เก่าได้
- npm release อิง tag

### วิธีสร้าง Tag

```bash
# Create tag
git tag v0.3.0

# Push tag
git push origin v0.3.0
```

---

## 4️⃣ Publish (ส่งของขึ้นโลก)

### npm publish ทำอะไร

เอา code → npm registry

user ใช้ผ่าน `npx` / `npm install`

📌 **ข้อควรรู้:**
- publish ซ้ำ version เดิม **ไม่ได้**
- ต้อง bump version ก่อนทุกครั้ง

### Release Process

```bash
# 1. Bump version
npm version patch  # หรือ minor, major

# 2. Build
npm run build

# 3. Publish
npm publish
```

---

## 5️⃣ Rollback (แผนหนีทีไล่)

### Rollback คือ

**"เผลอปล่อยของพัง → ทำยังไง"**

### วิธีหลัก ๆ

#### 1. Deprecate version

```bash
npm deprecate zend-theme@0.3.0 "broken add command"
```

#### 2. Patch ใหม่

```
0.3.0 ❌
0.3.1 ✅ fix
```

❌ **ลบ package ไม่ควรทำ**

---

## 🔗 ทุกอย่างเชื่อมกันยังไง

```
code change
  ↓
decide semver
  ↓
update changelog
  ↓
git tag
  ↓
npm publish
  ↓
user update
```

---

## 🧠 Mental Model ที่คุณควรจำ

### 1️⃣ คุณรับผิดชอบ behavior เดิม

ถ้า user เคยพิมพ์:
```bash
zend-theme add button
```

มัน **ต้องยังทำงาน**

### 2️⃣ Version = สัญญา

เลขเวอร์ = คำสัญญากับ user

### 3️⃣ อย่ากลัว release

**release บ่อย > release ช้า**

---

## 🏆 เทียบระดับคุณตอนนี้

| สิ่ง | สถานะ |
|-----|-------|
| CLI architecture | ✅ |
| dependency system | ✅ |
| config | ✅ |
| release mindset | 🔥 |

คุณอยู่ระดับเดียวกับ:
- shadcn/ui
- eslint
- tsup

แค่ scale ยังเล็ก

---

## 📋 Checklist สำหรับ Release

### ก่อน Release

- [ ] ทดสอบ command ทั้งหมด
- [ ] อัปเดต CHANGELOG.md
- [ ] ตรวจสอบ version ใน package.json
- [ ] Build และทดสอบ dist/

### Release Process

- [ ] `npm version <patch|minor|major>`
- [ ] `git push && git push --tags`
- [ ] `npm run build`
- [ ] `npm publish`

### หลัง Release

- [ ] ตรวจสอบ npm registry
- [ ] ทดสอบ install จาก npm
- [ ] แจ้ง user (ถ้าจำเป็น)

---

## 🎯 Best Practices

1. **Release บ่อย**: patch release สำหรับ bug fix
2. **Document ทุก change**: เขียน changelog ให้ชัดเจน
3. **Test ก่อน publish**: ทดสอบทุก command
4. **Version bump ตาม SemVer**: อย่าเพิ่ม version ตามใจ
5. **Tag ทุก release**: เพื่อ rollback ได้

---

## 📚 Resources

- [Semantic Versioning](https://semver.org/)
- [npm version](https://docs.npmjs.com/cli/v8/commands/npm-version)
- [npm publish](https://docs.npmjs.com/cli/v8/commands/npm-publish)
